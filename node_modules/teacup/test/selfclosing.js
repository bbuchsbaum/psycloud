// Generated by CoffeeScript 1.6.3
(function() {
  var br, expect, img, link, renderable, _ref;

  expect = require('expect.js');

  _ref = require('..'), renderable = _ref.renderable, img = _ref.img, br = _ref.br, link = _ref.link;

  describe('Self Closing Tags', function() {
    describe('<img />', function() {
      it('should render', function() {
        return expect(renderable(img)()).to.equal('<img />');
      });
      it('should render with attributes', function() {
        return expect(renderable(img)({
          src: 'http://foo.jpg.to'
        })).to.equal('<img src="http://foo.jpg.to" />');
      });
      return it('should throw when passed content', function() {
        return expect(renderable(function() {
          return img('with some text');
        })).to.throwException(/must not have content/);
      });
    });
    describe('<br />', function() {
      return it('should render', function() {
        return expect(renderable(br)()).to.equal('<br />');
      });
    });
    return describe('<link />', function() {
      return it('should render with attributes', function() {
        return expect(renderable(link)({
          href: '/foo.css',
          rel: 'stylesheet'
        })).to.equal('<link href="/foo.css" rel="stylesheet" />');
      });
    });
  });

}).call(this);
