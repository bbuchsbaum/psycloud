// Generated by CoffeeScript 1.6.3
(function() {
  var expect, render, tag, _ref;

  expect = require('expect.js');

  _ref = require('..'), render = _ref.render, tag = _ref.tag;

  describe('custom tag', function() {
    it('should render', function() {
      var template;
      template = function() {
        return tag('custom');
      };
      return expect(render(template)).to.equal('<custom></custom>');
    });
    it('should render empty given null content', function() {
      var template;
      template = function() {
        return tag('custom', null);
      };
      return expect(render(template)).to.equal('<custom></custom>');
    });
    it('should render with attributes', function() {
      var template;
      template = function() {
        return tag('custom', {
          foo: 'bar',
          ping: 'pong'
        });
      };
      return expect(render(template)).to.equal('<custom foo="bar" ping="pong"></custom>');
    });
    return it('should render with attributes and content', function() {
      var template;
      template = function() {
        return tag('custom', {
          foo: 'bar',
          ping: 'pong'
        }, 'zag');
      };
      return expect(render(template)).to.equal('<custom foo="bar" ping="pong">zag</custom>');
    });
  });

}).call(this);
