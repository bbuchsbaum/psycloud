// Generated by CoffeeScript 1.6.3
(function() {
  var expect, h1, render, _ref;

  expect = require('expect.js');

  _ref = require('..'), render = _ref.render, h1 = _ref.h1;

  describe('Context data', function() {
    return it('is an argument to the template function', function() {
      var template;
      template = function(_arg) {
        var foo;
        foo = _arg.foo;
        return h1(foo);
      };
      return expect(render(template, {
        foo: 'bar'
      })).to.equal('<h1>bar</h1>');
    });
  });

  describe('Local vars', function() {
    return it('are in the template function closure', function() {
      var obj, template;
      obj = {
        foo: 'bar'
      };
      template = function() {
        return h1("dynamic: " + obj.foo);
      };
      expect(render(template)).to.equal('<h1>dynamic: bar</h1>');
      obj.foo = 'baz';
      return expect(render(template)).to.equal('<h1>dynamic: baz</h1>');
    });
  });

}).call(this);
