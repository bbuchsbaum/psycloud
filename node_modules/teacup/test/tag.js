// Generated by CoffeeScript 1.6.3
(function() {
  var div, expect, p, renderable, script, _ref;

  expect = require('expect.js');

  _ref = require('..'), renderable = _ref.renderable, p = _ref.p, div = _ref.div, script = _ref.script;

  describe('tag', function() {
    it('renders text verbatim', function() {
      return expect(renderable(p)('foobar')).to.equal('<p>foobar</p>');
    });
    it('renders numbers', function() {
      expect(renderable(p)(1)).to.equal('<p>1</p>');
      return expect(renderable(p)(0)).to.equal('<p>0</p>');
    });
    it('renders Dates', function() {
      var date;
      date = new Date(2013, 1, 1);
      return expect(renderable(p)(date)).to.equal("<p>" + (date.toString()) + "</p>");
    });
    it("renders undefined as ''", function() {
      return expect(renderable(p)(void 0)).to.equal("<p></p>");
    });
    return it('renders empty tags', function() {
      var template;
      template = renderable(function() {
        return script({
          src: 'js/app.js'
        });
      });
      return expect(template()).to.equal('<script src="js/app.js"></script>');
    });
  });

}).call(this);
