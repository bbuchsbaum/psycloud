// Generated by CoffeeScript 1.6.3
(function() {
  var doctype, expect, render, _ref;

  expect = require('expect.js');

  _ref = require('..'), render = _ref.render, doctype = _ref.doctype;

  describe('doctype', function() {
    it('default should render html5 doctype', function() {
      var template;
      template = function() {
        return doctype();
      };
      return expect(render(template)).to.equal('<!DOCTYPE html>');
    });
    it('xml should render xml header', function() {
      var template;
      template = function() {
        return doctype('xml');
      };
      return expect(render(template)).to.equal('<?xml version="1.0" encoding="utf-8" ?>');
    });
    it('5 should render html 5 doctype', function() {
      var template;
      template = function() {
        return doctype(5);
      };
      return expect(render(template)).to.equal('<!DOCTYPE html>');
    });
    return it('transitional should render transitional doctype', function() {
      var template;
      template = function() {
        return doctype('transitional');
      };
      return expect(render(template)).to.equal('<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">');
    });
  });

}).call(this);
