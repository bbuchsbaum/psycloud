// Generated by CoffeeScript 1.6.3
(function() {
  var div, expect, p, render, _ref;

  expect = require('expect.js');

  _ref = require('..'), render = _ref.render, div = _ref.div, p = _ref.p;

  describe('nesting templates', function() {
    return it('renders nested template in the same output', function() {
      var nameHelper, template, user;
      user = {
        first: 'Huevo',
        last: 'Bueno'
      };
      nameHelper = function(user) {
        return p("" + user.first + " " + user.last);
      };
      template = function(user) {
        return div(function() {
          return nameHelper(user);
        });
      };
      return expect(render(template, user)).to.equal('<div><p>Huevo Bueno</p></div>');
    });
  });

}).call(this);
