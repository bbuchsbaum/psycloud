// Generated by CoffeeScript 1.6.3
(function() {
  var a, cede, div, expect, p, raw, render, strong, _ref;

  expect = require('expect.js');

  _ref = require('..'), render = _ref.render, raw = _ref.raw, cede = _ref.cede, div = _ref.div, p = _ref.p, strong = _ref.strong, a = _ref.a;

  describe('render', function() {
    return describe('nested in a template', function() {
      it('returns the nested template without clobbering the parent result', function() {
        var template;
        template = function() {
          return p(function() {
            return raw("This text could use " + (render(function() {
              return strong(function() {
                return a({
                  href: '/'
                }, 'a link');
              });
            })) + ".");
          });
        };
        return expect(render(template)).to.equal('<p>This text could use <strong><a href="/">a link</a></strong>.</p>');
      });
      return it('is aliased as cede', function() {
        var template;
        template = function() {
          return p(function() {
            return raw("This text could use " + (cede(function() {
              return strong(function() {
                return a({
                  href: '/'
                }, 'a link');
              });
            })) + ".");
          });
        };
        return expect(render(template)).to.equal('<p>This text could use <strong><a href="/">a link</a></strong>.</p>');
      });
    });
  });

}).call(this);
