(function() {
  var CORE_MODULES, resolve;

  resolve = require('resolve');

  CORE_MODULES = require('./core-modules');

  module.exports = function(x) {
    return x === 'browser-builtins' || (resolve.isCore(x)) || [].hasOwnProperty.call(CORE_MODULES, x);
  };

}).call(this);

/*
//@ sourceMappingURL=is-core.js.map
*/