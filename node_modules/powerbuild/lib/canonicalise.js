(function() {
  var path;

  path = require('path');

  module.exports = function(root, file) {
    var rv;
    root = path.resolve(root);
    rv = ("" + (path.relative(root, file))).replace(/\\/g, '/');
    return rv;
  };

}).call(this);

/*
//@ sourceMappingURL=canonicalise.js.map
*/