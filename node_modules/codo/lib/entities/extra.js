// Generated by CoffeeScript 1.6.3
(function() {
  var Entities, FS, Markdown, Path;

  FS = require('fs');

  Path = require('path');

  Entities = require('../_entities');

  Markdown = require('../tools/markdown');

  module.exports = Entities.Extra = (function() {
    function Extra(environment, path) {
      this.environment = environment;
      this.path = path;
      this.name = Path.relative(this.environment.options.basedir, this.path);
      this.content = FS.readFileSync(this.path, 'utf-8');
      this.parsed = /\.(markdown|md)$/.test(this.path) ? Markdown.convert(this.content) : "<p>" + this.content.replace(/\n/g, '<br/>') + "</p>";
    }

    Extra.prototype.linkify = function() {};

    Extra.prototype.inspect = function() {
      return {
        path: this.path,
        parsed: this.parsed
      };
    };

    return Extra;

  })();

}).call(this);
